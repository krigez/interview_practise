// Write a function  that gets an array of strings and returns the longest common prefix.

// For example,

// ["somehow", "somewhat", "something", "some"]  ->  "some"
// ["maximum", "minimum"]  ->  "m"
// ["llo", "lloon"]  ->  "llo"

const longestCommonPrefix = (arr) => {
  return arr[0];
};
